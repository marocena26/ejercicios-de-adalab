'use strict';

const nameUser = 'María';


if (nameUser === 'María') {
    console.log('Bienvenida, ' + nameUser); 
  //Esta línea se ejecuta solo si se cumple la condición
  } else {
    console.log('Lo siento pero el usuario que has introducido no está registrado'); 
  //Esta línea se ejecuta solo si se NO cumplen ninguna de las condiciones anteriores
  }


