"use strict";

const dimension = (borderBox,width ,padding,borderSize) => {
    const borderBox = true;
    const width  = 34; 
    const padding = 30;
    const borderSize = width*2 + padding*2;
    return 'El ancho del contenido es:' width + ' y el ancho total de la caja es: ' + borderSize;
}



